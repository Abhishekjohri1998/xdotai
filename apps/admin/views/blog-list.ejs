<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Outfit:wght@600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/admin.css">
</head>

<body>
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <a href="./" class="admin-logo"><span class="logo-x">X</span> DOT AI <span
                    class="admin-badge">BLOG</span></a>
            <nav class="admin-nav">
                <a href="./" class="admin-nav-link">📊 Dashboard</a>

                <a href="./blog" class="admin-nav-link active">📝 All Posts</a>

                <a href="./blog/new" class="admin-nav-link">✨ New Post</a>

                <a href="/blogs" target="_blank" class="admin-nav-link">🌐 View Blog</a>

                <hr style="border-color: var(--border-subtle); margin: 1rem 0;">
                <span class="admin-nav-label"
                    style="padding: 0.5rem 0.75rem; font-size: 0.7rem; color: var(--text-muted);">CATEGORIES</span>
                <% categories.forEach(cat=> { %>
                    <div style="display:flex;align-items:center;justify-content:space-between;padding:0.25rem 0.75rem;">
                        <span style="font-size:0.8rem;color:var(--text-secondary);">
                            <%= cat.name %>
                        </span>
                        <form method="POST" action="./blog/category/<%= cat.id %>/delete" style="margin:0;"
                            onsubmit="return confirm('Delete category?')">
                            <button type="submit"
                                style="background:none;border:none;color:var(--text-muted);cursor:pointer;font-size:0.7rem;">✕</button>
                        </form>
                    </div>
                    <% }) %>
                        <form method="POST" action="./blog/category" style="padding:0.5rem 0.75rem;">

                            <div style="display:flex;gap:4px;">
                                <input type="text" name="name" placeholder="New category..." required
                                    style="flex:1;font-size:0.75rem;padding:0.3rem 0.5rem;">
                                <button type="submit" class="admin-btn admin-btn-primary"
                                    style="padding:0.3rem 0.5rem;font-size:0.7rem;">+</button>
                            </div>
                        </form>
            </nav>
            <div class="admin-sidebar-footer">
                <span>Logged in as admin</span>
                <a href="./logout">Logout →</a>

            </div>
        </aside>

        <main class="admin-main">
            <div class="admin-header">
                <h1>📝 Blog Manager</h1>
                <a href="./blog/new" class="admin-btn admin-btn-primary">✨ New Post</a>

            </div>

            <!-- Stats -->
            <div class="blog-stats-grid">
                <div class="blog-stat-card">
                    <span class="blog-stat-number">
                        <%= stats.total %>
                    </span>
                    <span class="blog-stat-label">Total Posts</span>
                </div>
                <div class="blog-stat-card blog-stat-published">
                    <span class="blog-stat-number">
                        <%= stats.published %>
                    </span>
                    <span class="blog-stat-label">Published</span>
                </div>
                <div class="blog-stat-card blog-stat-draft">
                    <span class="blog-stat-number">
                        <%= stats.drafts %>
                    </span>
                    <span class="blog-stat-label">Drafts</span>
                </div>
                <div class="blog-stat-card blog-stat-featured">
                    <span class="blog-stat-number">
                        <%= stats.featured %>
                    </span>
                    <span class="blog-stat-label">Featured</span>
                </div>
            </div>

            <!-- Posts List -->
            <div class="admin-card">
                <% if (posts.length===0) { %>
                    <div style="text-align:center;padding:3rem;color:var(--text-muted);">
                        <p style="font-size:2rem;">📝</p>
                        <h3>No blog posts yet</h3>
                        <p>Create your first blog post to get started!</p>
                        <a href="./blog/new" class="admin-btn admin-btn-primary" style="margin-top:1rem;">✨ Create

                            First Post</a>
                    </div>
                    <% } else { %>
                        <div class="blog-posts-table">
                            <div class="blog-table-header">
                                <span style="flex:3;">Title</span>
                                <span style="flex:1;">Category</span>
                                <span style="flex:0 0 80px;text-align:center;">Status</span>
                                <span style="flex:0 0 60px;text-align:center;">⭐</span>
                                <span style="flex:1;">Date</span>
                                <span style="flex:0 0 120px;text-align:right;">Actions</span>
                            </div>
                            <% posts.forEach(post=> { %>
                                <div class="blog-table-row">
                                    <div style="flex:3;display:flex;align-items:center;gap:0.75rem;">
                                        <% if (post.featured_image) { %>
                                            <img src="<%= post.featured_image %>" alt="" class="blog-table-thumb">
                                            <% } else { %>
                                                <div class="blog-table-thumb blog-table-thumb-empty">📄</div>
                                                <% } %>
                                                    <div>
                                                        <a href="./blog/<%= post.slug %>/edit" class="blog-table-title">
                                                            <%= post.title %>
                                                        </a>
                                                        <small style="color:var(--text-muted);display:block;">
                                                            <%= post.reading_time %> min read
                                                        </small>
                                                    </div>
                                    </div>
                                    <span style="flex:1;"><span class="blog-category-pill">
                                            <%= post.category || 'Uncategorized' %>
                                        </span></span>
                                    <span style="flex:0 0 80px;text-align:center;">
                                        <span class="blog-status-badge blog-status-<%= post.status %>">
                                            <%= post.status %>
                                        </span>
                                    </span>
                                    <span style="flex:0 0 60px;text-align:center;">
                                        <button class="blog-featured-btn <%= post.is_featured ? 'active' : '' %>"
                                            onclick="toggleFeatured(<%= post.id %>, this)" title="Toggle featured">
                                            <%= post.is_featured ? '⭐' : '☆' %>
                                        </button>
                                    </span>
                                    <span style="flex:1;font-size:0.8rem;color:var(--text-secondary);">
                                        <%= post.published_at ? new Date(post.published_at).toLocaleDateString('en-US',
                                            {month:'short',day:'numeric',year:'numeric'}) : '—' %>
                                    </span>
                                    <div style="flex:0 0 120px;display:flex;gap:0.5rem;justify-content:flex-end;">
                                        <a href="./blog/<%= post.slug %>/edit"
                                            class="admin-btn admin-btn-secondary admin-btn-small">Edit</a>
                                        <form method="POST" action="./blog/<%= post.slug %>/delete" style="margin:0;"
                                            onsubmit="return confirm('Delete this post?')">
                                            <button type="submit" class="admin-btn admin-btn-small"
                                                style="background:rgba(239,68,68,0.15);color:#ef4444;border:1px solid rgba(239,68,68,0.3);">✕</button>
                                        </form>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
                        <% } %>
            </div>
        </main>
    </div>

    <script>
        async function toggleFeatured(id, btn) {
            try {
                const res = await fetch(`/api/blog/toggle-featured/${id}`, { method: 'POST' });
                const data = await res.json();
                if (data.success) {
                    btn.textContent = data.is_featured ? '⭐' : '☆';
                    btn.classList.toggle('active', data.is_featured);
                }
            } catch { }
        }
    </script>
</body>

</html>